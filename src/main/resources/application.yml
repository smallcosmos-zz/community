spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql:///community?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    #   数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
  thymeleaf:
    cache: false

mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml

github:
  client:
    id: 04756ad84718e094174b
    secret: 00f94308d8bad0203575cbd5fed04d28cc73f5b0
  redirect:
    uri: http://localhost:8080/callback

aliyun:
  AccessKey:
    ID: LTAI4FcnPBfgNQY3PMZcnDgU
    Secret: aqcK2EfeAkplKE2uoSxTWrwCZyuQXP
  endpoint: http://oss-cn-beijing.aliyuncs.com
  bucketName: smallcosmos
logging:
  path: logs
  file:
    max-size: 200MB
    max-history: 30
  level:
    root: info
    com.hmily.community.mapper: debug
